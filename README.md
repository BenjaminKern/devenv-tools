# devenv-tools

Personal dev environment bootstrap. Bundles CLI tools, shell config, git config, and neovim setup into a single portable install.

## Install

```sh
# macOS / Linux
./get_devenv_tools.sh ~/devenv

# Windows
get_devenv_tools.bat C:\devenv
```
